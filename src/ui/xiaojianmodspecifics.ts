/**
 * Applies styling modifications for a glossy effect on buttons.
 * This is achieved by injecting a CSS style block into the document's head.
 */
export function applyGlossyButtonStyling(): void {
  // All styling has been moved to main.scss to ensure compatibility.
}
